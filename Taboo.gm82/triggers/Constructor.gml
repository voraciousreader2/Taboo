/*
Called mnaually when using the contruct() system.
Example:
with (instance_create(x,y,Thing)) {
    var1=2
    tag="bepp"
    construct()
}
Then, on object Thing, the Contructor can change instance properties based on var1 and tag.
*/

return 0
